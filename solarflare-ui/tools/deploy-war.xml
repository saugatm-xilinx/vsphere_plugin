<!--
   Shortcut to invoke the build script with the deploy-bundle target.
-->
<project name="solarflare-ui-deploy" default="deploy-bundle">
   <target name="deploy-bundle">
       <ant antfile="build-war.xml" target="deploy-bundle"/>
   </target>
</project>