<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xmlns:osgi="http://www.springframework.org/schema/osgi"
   xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                      http://www.springframework.org/schema/osgi http://www.springframework.org/schema/osgi/spring-osgi.xsd">

   <!-- definitions using elements of the osgi namespace can be included
      in this file. There is no requirement to keep these definitions
      in a separate file if you do not want to. The rationale for
      keeping these definitions separate is to facilitate integration
      testing of the bundle outside of an OSGi container -->

  <!-- References to platform services used by the controllers -->
        
         <!-- added for solarflare -->
	<osgi:service id="HostAdapterService" ref="HostAdapterServiceImpl"
		interface="com.solarflare.vcp.services.HostAdapterService" />

	<!-- References to platform services used by the controllers -->
	<osgi:reference id="userSessionService"
         interface="com.vmware.vise.usersession.UserSessionService"/>
   
   <osgi:service id="SfVimService" ref="SfVimServiceImpl" 
	    interface="com.solarflare.vcp.vim.SfVimService" />
   
<!--    <osgi:service id="connection" ref="ConnectionImpl"
		interface="com.solarflare.vcp.vim.connection.Connection" /> -->
		
</beans>